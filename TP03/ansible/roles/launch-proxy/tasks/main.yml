---
# tasks file for roles/launch-proxy
  - name: Run HTTPD
    docker_container:
      name: httpd
      image: louischarnay/tp01-web:latest
      state: restarted
      networks:
        - name: tp03-network
      ports:
        - "80:80"