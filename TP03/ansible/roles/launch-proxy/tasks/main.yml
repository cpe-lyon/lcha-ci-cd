---
# tasks file for roles/launch-proxy
  - name: Run HTTPD
    docker_container:
      name: httpd
      image: louischarnay/tp01-web:1.0
      state: started
      networks:
        - name: tp03-network
      ports:
        - "80:80"